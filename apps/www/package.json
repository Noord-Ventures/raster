{
  "name": "www",
  "version": "0.2.0",
  "private": true,
  "description": "raster.design — documentation and component gallery for the Raster design system.",
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --filter @raster/core build && pnpm --filter @raster/core build:registry && pnpm --filter @raster/react build && node scripts/copy-registry.mjs",
    "predev": "node scripts/copy-registry.mjs",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@raster/core": "workspace:*",
    "@raster/react": "workspace:*",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "typescript": "^5.7.3"
  }
}
